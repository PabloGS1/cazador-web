# Credenciales locales: nunca al repo
secrets.yaml
cv.txt

# Python
__pycache__/
*.pyc

# Datos intermedios: raw se regenera en cada run
web/data/raw_jobs.json
# Features (independiente del perfil) - grande, se regenera en cada matcher
web/data/features.json

# Worker
worker/node_modules/
worker/.wrangler/
worker/seed.sql
worker/seed/
worker/.dev.vars
